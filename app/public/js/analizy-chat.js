// Czat analiz — SZKIELET (majster, fala A). Właściwą implementację wnosi Z20 (fala B);
// plik istnieje już teraz, żeby SHELL w sw.js i strażnik preflight były spójne między falami.
export function initChat() { /* Z20 */ }
